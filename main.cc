#include <bits/stdc++.h>
#include <iostream>

#include "demos/exceptions/exceptions.hpp"

int main(int argc, char **argv)
{
    static_cast<void>(argc);
    static_cast<void>(argv);

    demos::exceptions::run_demo();

    return 0;
}